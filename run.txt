## 先跑一段时间的fuzz，得到初始corpus

## 得到初始种子后，跑tracer获取覆盖信息，得到瓶颈
## tracer加入实时监测覆盖率情况，如果覆盖率一段时间没有增长则开始后续突破流程

## 瓶颈选择（直接通过排序选）

## 通过选择的瓶颈位置获取瓶颈点分支条件和对应种子

## 运行DSE，传入参数

## 检查DSE生成的结果是否能够突破，能突破就加入corpus

## 不能突破就调用大模型，利用tree-sitter的AST进行切片

## 切片结果连同其他必要输入一起提供给LLM交互（附带重试次数）